import { Cinzel, Bitcount_Prop_Single } from "next/font/google";

export const cinzel = Cinzel({ subsets: ["latin"] });
export const bitcountPropSingle = Bitcount_Prop_Single({ subsets: ["latin"] });
